name: Verify EsPCEx 2022 definitive expansion

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/expand-espcex-2022.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: npm ci

      - name: Apply 2022 definitive dataset and metadata
        run: python .github/scripts/expand-espcex-2022.py

      - name: Verify tests and build
        run: |
          npm test
          npm run build

      - name: Commit verified expansion
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/providers/espcex/answer-keys.generated.json src/lib/providers/espcex/espcex.test.ts src/lib/sources/index.ts src/lib/sources/sources.test.ts scripts/sources-audit.mjs docs/exam-sources.md
          git commit -m "feat(espcex): activate definitive 2022 edition [skip ci]"
          git pull --rebase origin main
          git push origin main
